title: Ben Notkin
name: Ben Notkin
markdown: kramdown

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"
    values:
      layout: "post"
      length: "short"
      scroll: "noScroll" # should the post scroll separately? use for especially long posts
      more: "false"

excerpt_separator: <!--more-->